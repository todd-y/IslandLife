public enum RoomType {
    Other = 1,
    Start,
    End,
}

public enum RoleType {
    None,
    Player,
    Monster,
    Battlery,
}

public enum GoalType {
    Collect,
    Time,
    Special,
}

public enum RoomState {
    Wait,
    Complete,
}