public enum RoleType {
    Warrior,
}

public enum EquipPosition {
    Weapon = 0,
    Armor,
    Jewelry,
}

public enum EquipType {
    Sword,
    Armor,
    Ring,
}

public enum Quality {
    White,
    Green,
    Blue,
    Purple,
    Orange
}

public enum Attribute {
    Atk,
    Hp,
    Mp,
}